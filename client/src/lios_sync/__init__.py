"""lios-sync — the local side-car daemon that syncs a Mac into lios.

Until 2026-09-02 this package was `comar` (Co-Managed Archive; from the Irish
'comar', a confluence). The house is still called comar; the platform is lios.
3.0.0 is the first release under the new name — 2.x daemons are frozen at 2.6.3
by the server (see api/v1.py heartbeat) until their machine is re-installed.
"""

__version__ = "3.1.0"
