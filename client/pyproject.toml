[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lios-sync"
dynamic = ["version"]
description = "Comar (Co-Managed Archive) — local side-car daemon: vault watcher, Apple Reminders dispatch via EventKit, Apple Health push"
requires-python = ">=3.11"
license = "MIT"

dependencies = [
    "click>=8.1",
    "httpx>=0.27",
    "watchdog>=4.0",
    "uvicorn[standard]>=0.30",
    "starlette>=0.37",
    "tomli>=2.0; python_version < '3.11'",
    "python-dotenv>=1.0",
    "packaging>=23.0",
    "pyobjc-framework-EventKit>=10.0; sys_platform == 'darwin'",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[project.scripts]
lios-sync = "lios_sync.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.hatch.version]
path = "src/lios_sync/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/lios_sync"]
