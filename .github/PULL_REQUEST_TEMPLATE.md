## What this changes and why

<!-- One or two sentences. Link an issue if there is one. -->

## How it was tested

<!-- e.g. "pytest -m 'not db' and -m 'db' both pass", "tested against a real Google Calendar account", "added a snapshot test" -->

## Checklist

- [ ] Tests pass locally (`pytest` in `server/backend` and `client`, `npm run build` in `server/frontend`)
- [ ] No new dependency under a copyleft license (GPL/AGPL/SSPL)
- [ ] No hardcoded personal data (names, locations, tokens) — examples use the existing fictional household
- [ ] Docs updated if this changes setup, config, or behavior described in a README/CLAUDE.md
