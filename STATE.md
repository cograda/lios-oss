# core — state of project

GENERATED by `scripts/state_of_project.py` — do not edit; run `make -C core state-of-project`.

| Metric | Count | Notes |
|---|---|---|
| Integration packages | 31 | dirs under `app/integrations/` with a `manifest.py`; disk-verifiable, environment-independent |
| MCP tools (this environment) | 179 | see warning below — environment-dependent, this is a lower bound |
| Registered integrations | 31 | via `register_all()` / `get_all()` |
| DB tables | 84 | `len(Base.metadata.tables)` after `import app.models` |
| Tests (unit tier, `-m "not db"`) | 1909 | |
| Tests (db tier, `-m db`) | 1286 | |
| Tests (total collected) | 3195 | |
| Alembic head revision | `e6b1f4a9c3d7` | |
| CI workflow files | 8 | repo-root `.github/workflows/*.yml` |
| Apps present (with Makefile) | 3 | `apps/*/Makefile`: dashboard, hub, loops |

⚠️ **The MCP tool count is environment-dependent, and a lower bound on this machine, not a fixed constant.** `sheets`, `transcription` and `vision` contributed 0 tools when this was measured — they are pure facades / caller-driven integrations, not (necessarily) unconfigured ones; a machine with no credentials configured would report fewer still, because real registration additionally gates on `is_configured()`, which this measurement does not apply. Zero-tool integrations in this run: `alerts`, `sheets`, `transcription`, `vision`.

Integration packages (31): `alerts`, `apple_health`, `apple_reminders`, `attachments`, `coffee`, `commute`, `embedding`, `finance`, `google_calendar`, `google_docs`, `google_mail`, `historical_corpus`, `homeassistant`, `household`, `inbox`, `irish_rail`, `lastfm`, `media`, `notifications`, `obsidian`, `sheets`, `signals`, `snags`, `solar_forecast`, `strava`, `system`, `tasks`, `transcription`, `vision`, `weather`, `whatsapp`

Registered integrations (31): `alerts`, `apple_health`, `apple_reminders`, `attachments`, `coffee`, `commute`, `embedding`, `finance`, `google_calendar`, `google_docs`, `google_mail`, `historical_corpus`, `homeassistant`, `household`, `inbox`, `irish_rail`, `lastfm`, `media`, `notifications`, `obsidian`, `sheets`, `signals`, `snags`, `solar_forecast`, `strava`, `system`, `tasks`, `transcription`, `vision`, `weather`, `whatsapp`

