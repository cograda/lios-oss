"""V4 kernel namespace.

Home for cross-integration kernel machinery that isn't specific to any one
integration package: the manifest schema (`manifest.py`) and startup
validation (`validate.py`). Later V4 chunks (1.2+) add auto-discovery,
registries, and dispatch here.
"""
