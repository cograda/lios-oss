# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# Node
node_modules/
frontend/dist/

# Environment
.env
.env.local
.env*.local

# Data
*.csv
*.json
*.zip
!package.json
!package-lock.json
!tsconfig.json
!docker-compose.yml
!scripts/**/*.json
!backend/tests/snapshots/*.json
!backend/app/integrations/commute/data/*.json
*.db
*.pkl

# Build artifacts
frontend/tsconfig.tsbuildinfo

# Certs
certs/*.pem
certs/*.key
certs/*.crt
certs/*.p12
!certs/README.md

# IDE
.vscode/
.idea/

# Claude
.claude/

# Docker
pgdata/

# `make stage-libs` copies lios/libs/coglib in here so the Dockerfile can COPY
# it (the build context cannot see ../../libs). Never commit a copy of a shared
# lib: the vendored copy this replaced was a second editable copy of coglib.
.libs/
